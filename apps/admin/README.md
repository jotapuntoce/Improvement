# Admin

Panel interno de Jose Carlos — cuentas Improvement (organizaciones reales), backlog de prospectos,
catálogo de PowerUps y detalle del Mapa de Construcción de cada organización.

Ver `CLAUDE.md` en la raíz del monorepo para comandos, arquitectura y convenciones. Desarrollo local:

```bash
pnpm dev:admin   # http://localhost:3100
```

Desplegado en Vercel con Root Directory = `apps/admin`.
