# dependencies
/node_modules
**/node_modules
/.pnp
.pnp.*

# testing
/coverage

# playwright (E3-T4 — primer spec E2E del repo)
**/test-results/
**/playwright-report/
**/blob-report/

# next.js
**/.next/
**/out/

# production
**/build
**/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.example es la única excepción committeada (blueprint §10, Files that must be committed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
**/next-env.d.ts

# turbo
.turbo

# superpowers:subagent-driven-development — ledger, briefs, reports, review packages por plan
# (scratch de ejecución, nunca parte del código; ver .superpowers/sdd/<plan>/progress.md)
.superpowers/

# superpowers:using-git-worktrees — worktrees aislados para ejecutar un plan (git worktree add
# manual, fallback porque EnterWorktree no resuelve el repo en este entorno)
.worktrees/
